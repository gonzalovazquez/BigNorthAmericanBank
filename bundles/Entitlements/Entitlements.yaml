swagger: '2.0'
info:
  contact:
    name: Eric Broda
    url: N/A
    email: ericbroda@rogers.com
  version: v3_0_0
  description: >-
    An Open Source API for Banks.  (c) TESOBE Ltd. 2011 - 2016. Licensed under
    the AGPL and commercial licences.  Decomposed from Master PSD2 Swagger
    (http://psd2.it/#psd2apps).  Generated by psd2-generator:
    https://github.com/ericbroda/psd2-generator
  title: 'API URI: /entitlements'
host: apisandbox.openbankproject.com
basePath: /obp/v3_0_0
schemes:
  - http
  - https
securityDefinitions:
  PSUOAuth2Security:
    type: oauth2
    flow: application
    tokenUrl: 'http://localhost:8080/oauth2/token'
    scopes:
      Entitlements.create: CREATE access to Entitlements
      Entitlements.read: READ access to Entitlements
      Entitlements.update: UPDATE access to Entitlements
      Entitlements.delete: DELETE access to Entitlements
      Entitlements.all: APP access to Entitlements
paths:
  /entitlements:
    get:
      tags:
        - '2_0_0'
      summary: Get all Entitlements
      description: <p>Login is required.</p>
      operationId: v2_0_0-getAllEntitlements
      parameters: []
      responses:
        '200':
          description: Success
          schema:
            required:
              - list
            properties:
              list:
                type: array
                items:
                  required:
                    - entitlement_id
                    - role_name
                    - bank_id
                  properties:
                    bank_id:
                      type: string
                      example: gh.29.uk
                    role_name:
                      type: string
                      example: CanQueryOtherUser
                    entitlement_id:
                      type: string
                      example: 6fb17583-1e49-4435-bb74-a14fe0996723
        '400':
          description: Error
          schema:
            properties:
              message:
                type: string
                example: 'OBP-20001: User not logged in. Authentication is required!'
      security:
        - PSUOAuth2Security:
            - Entitlements.all
